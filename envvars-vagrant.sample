export RAKE_ANSIBLE_USE_VAGRANT=1
export ANSIBLE_NFSCLIENT_VAGRANT_BOXNAME=centos/7 # name of the vagrant box to use for testing
export ANSIBLE_NFSCLIENT_VAGRANT_PROVIDER=libvirt     # name of the vagrant provider to use for testing
